fun = @(t,y) -20.*(y-2);
y0 = 3;
a=0;
b=2;
h1=1/25;
h2=1/20;
h3=1/15;
h4=1/10;

Y=BDF(fun,a,b,y0,h1);
tocnar = @(x) 2+exp(-20*x);

st_iteracij = (b-a)/h;
X = linspace(a,b,st_iteracij+1); %vrstica, ki vsebuje x_i
